{
  "name": "@bhzd/use-emotion",
  "version": "1.0.0",
  "description": "wrapper react  hook for emotion js",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && babel src/lib --out-dir dist --copy-files"
  },
  "presets": [  
    "@babel/preset-react"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.19.3"
  }
}
